export const MAX_PERSON_RATING = 1000;
export const MIN_PERSON_RATING = 0;

export const GENDER_CIS = ["m", "f"] as const;
export const GENDER_IDENTITY = ["m", "f", "nb"] as const;

export const GENDER_SETTING = ["traditional", "modern"] as const;
